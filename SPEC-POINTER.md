# Feature 009 — Transport Admin Dashboard: Live Order Lifecycle

This repository's work for feature 009 is planned and tracked entirely in the platform
repository, not here. There is no parallel spec in this repo, by design (see the feature's
spec.md, Clarifications, Q2).

- **Spec, plan, tasks, contracts**: `ciro_fuel/specs/009-transport-dashboard-order-lifecycle/`
- **Branch**: `009-transport-dashboard-order-lifecycle` (this branch, matching the platform repo's)
- **This repo's role**: implement the dashboard-side tasks in `tasks.md`, prefixed `web_dashboard/`

Do not create a `specs/009-...` directory in this repository — it would fork the source of
truth the two repos are required to share.
