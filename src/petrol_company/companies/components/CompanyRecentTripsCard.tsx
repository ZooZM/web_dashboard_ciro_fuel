// Feature 013 T088/FR-048: no per-transporter trip/order log endpoint exists anywhere on
// the platform — removed outright rather than left showing fabricated rows, same
// precedent as `AvailableBalanceCard.tsx`/`DueLitersBalanceCard.tsx`.
export function CompanyRecentTripsCard() {
  return null;
}
